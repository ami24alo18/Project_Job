import { Alert,Button,Chip,Paper,Stack,Typography } from '@mui/material';import { useEffect,useState } from 'react';import { Link as RouterLink } from 'react-router-dom';import { applicationPackagesApi } from '../api/applicationPackagesApi';import type { ApplicationHandoff } from '../types/applicationPackages';
export function ApplicationHandoffsPage(){const[x,setX]=useState<ApplicationHandoff[]>([]);useEffect(()=>{void applicationPackagesApi.handoffs().then(r=>setX(r.content))},[]);return <Stack spacing={2}><Typography variant="h4">Application handoffs</Typography>{x.length===0?<Alert severity="info">No manual handoffs yet.</Alert>:x.map(h=><Paper key={h.id} sx={{p:2}}><Stack direction="row" justifyContent="space-between"><div><Typography variant="h6">{h.jobTitle} — {h.company}</Typography><Chip label={h.status}/></div><Button component={RouterLink} to={`/application-handoffs/${h.id}`}>Open</Button></Stack></Paper>)}</Stack>}
